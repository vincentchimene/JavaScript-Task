// A JavaScript program that asks user's favourite colour and prints it.
const prompt = require("prompt-sync")();

let color = prompt("What is your favourite color? ");
console.log(`Your favourite color is ${color}`);
